sap.ui.define(['sap/fe/core/AppComponent'], function (AppComponent) {
    'use strict';

    return AppComponent.extend("myapp-fiori.Component", {
        metadata: {
            manifest: "json"
        }
    });
});
